name: Generate output files

on: [push]
  # push:
  #   tags:
  #   - v*

jobs:
  # Generate-local:
  #   runs-on: ubuntu-latest
  #   container:
  #     image: kicad/kicad:$KICAD_TAG
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4

  Generate-demo:
    runs-on: ubuntu-latest
    container:
      image: kicad/kicad:nightly-full
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - run: ls
    - run: pwd
    # - name: Generate files
    #   run: |
    #     cp -r /usr/share/kicad/demos/video/ ./
